# ThinkStill 400 — TRUE ONE-OF-ONE PRODUCT-READY LOCKED

Complete GitHub package generated directly from:

`ThinkStill_400_Rituals_TRUE_ONE_OF_ONE_PRODUCT_READY_LOCKED_MASTER(2).xlsx`

Upload the **contents of this folder to the root** of your existing
`ramkumarjothi1982/thinkstill-rituals` repository.

## Consumer experience

The renderer is configured for the locked Friend Edition:

**Emotional-distress cue → Ritual Title / Play Time → ThinkStill Friend Ritual + Steps → Friend Support → One-Line WIN → If This Ritual Doesn't Help → Formula Flow → Safety**

### Important: do not render a second Goal

The workbook already carries the goal/context naturally inside
`ThinkStill Friend Ritual + Steps`.

These fields are internal only and must never be shown directly:

- `hook_internal`
- `goal_internal`
- `game_move_internal`
- `source_trigger`
- `source_pattern`

## Five Friend Support fields

The JSON keeps both descriptive Friend Edition names and compatibility aliases:

1. `breath_support_friend`
2. `mindfulness_support_friend`
3. `positive_psychology_friend`
4. `contemplative_support_friend`
5. `friend_echo_affirmation`

The complete optional stack is in:

`optional_friend_support_stack`

## Production routing

Use only the balanced release routing:

- `release_routing_trigger`
- `secondary_routing_trigger`
- `release_routing_pattern`

Final routing:

- 22 release triggers
- 18–19 primary rituals per trigger
- exactly 20 eligible rituals per trigger including secondary routing
- 63 release patterns
- 6–7 rituals per release pattern
- global no-replay cycle across all 400 rituals

Do not rebalance or route from `source_trigger` / `source_pattern`.

## Bubble files

- `glitch.json` — 58
- `drop.json` — 57
- `loopie.json` — 57
- `patch.json` — 57
- `rush.json` — 57
- `still.json` — 57
- `sync.json` — 57

Total: **400 rituals**

## Included files

- `manifest.json`
- `glitch.json`
- `drop.json`
- `loopie.json`
- `patch.json`
- `rush.json`
- `still.json`
- `sync.json`
- `ThinkStillUnifiedChat.tsx`
- `ThinkStillUnifiedChat.txt`
- `validate.js`
- `package.json`
- `.gitignore`
- `README.md`
- `DATA_SCHEMA.md`
- `DEPLOY.md`
- `ThinkStill_400_Rituals_TRUE_ONE_OF_ONE_PRODUCT_READY_LOCKED_MASTER.xlsx`

## Validate

```bash
npm run validate
```

Expected result:

```text
VALIDATION PASSED — ThinkStill TRUE ONE-OF-ONE LOCKED
```

## Raw manifest URL

```text
https://raw.githubusercontent.com/ramkumarjothi1982/thinkstill-rituals/main/manifest.json
```

## Locked release rule

The 400-ritual dataset is locked. Future changes should be UI/integration changes
unless you intentionally decide to revise the ritual library itself.
