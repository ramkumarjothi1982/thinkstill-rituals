# v13 release notes

## Locked content

- 750 rituals, TS-001 through TS-750.
- Permanent source master: `ThinkStill_750_v13_FINAL_RELEASE_PERMANENT_10of10.xlsx`.
- Engine fingerprints verified against the current Primary Game Engine text.
- Zero exact duplicate core fields.
- Zero repeated 4-word core fragments.
- Mind Bend and Safety release gates passed in the final workbook audit.
- 25 SUPPORT FIRST rituals, all `RIGHT NOW`.

## GitHub package additions

- Seven Bubble JSON libraries.
- One-folder v13 manifest.
- Combined library JSON.
- 22 broad routing parents generated from the final `Best For` taxonomy.
- 73 precise subpatterns retained from exact `Best For` labels.
- Release validation script and GitHub Actions workflow.
- Latest Framer Reset Console source patched for the v13 `GOAL` schema.

## UI compatibility patch

The source UI previously required a legacy `MISSION` heading before using an exact complete ritual. v13 uses `GOAL`. The GitHub component now accepts `GOAL` so it displays the locked full ritual directly rather than rebuilding it through a legacy fallback.


## v38.11 semantic routing fix

- Added a dedicated `urge` intent so broad entries such as `drugs`, `weed`, `alcohol`, `smoking`, `vaping`, `cocaine`, `relapse`, and `craving` no longer fall into the generic neutral route.
- Added semantic alias expansion from everyday user language into the locked workbook’s canonical 22-parent routing vocabulary.
- Added high-confidence aliases for substance use/habit loops, relationship loss/conflict, money stress, work/study performance, trauma/flashback imagery, loneliness/rejection, and sleep language.
- Preserved the full 750-ritual global no-replay cycle and all existing UI/animation behavior.
- Immediate body-alarm and acute-danger language still overrides generic substance routing; overdose and severe withdrawal remain inside SUPPORT FIRST handling.
- Router storage version advanced from `v1900` to `v2000` so stale pre-fix route state cannot contaminate the corrected release.


## v38.12 growth release

- Kept all 750 v13 ritual strings frozen.
- Added 50 curated first-session picks with relevance-safe routing boost for the first three normal serves.
- Added SHARE REVEAL Mind Bend cards (1080×1920) with private challenge excluded.
- Added `?try=TS-xxx` deep links that open the exact shared ritual.
- Added DID IT SHIFT? outcome feedback with four levels.
- Added bounded adaptive routing by Reset DNA family.
- Added YOUR RESET DNA to the Vault after three ratings.
- Added unseen-reset discovery count.
- Added privacy-preserving local growth events plus `thinkstill:growth` CustomEvents for analytics integration.
- Added `growth-layer-v1.json` and a GROWTH LAYER sheet to the source workbook.
