# ThinkStill v58.20.7 — FINAL NO-TIPS GitHub Package

This is the runtime GitHub package for the locked 750-ritual release.

## Upload every file in this folder

Keep all files together in the repository root (or the same GitHub folder). The Framer component reads `manifest.json` and the seven bubble JSON files.

Existing repository raw manifest URL:

`https://raw.githubusercontent.com/ramkumarjothi1982/thinkstill-rituals/refs/heads/main/manifest.json`

## Required by Framer

- `manifest.json`
- `rituals_glitch.json`
- `rituals_drop.json`
- `rituals_still.json`
- `rituals_patch.json`
- `rituals_loopie.json`
- `rituals_rush.json`
- `rituals_sync.json`
- `ThinkStill_UnifiedChat_v58_20_7_FINAL_RELEASE.tsx`

## Additional runtime/release files

- `rituals.json` — all 750 runtime rituals
- `routing.json` — canonical mapping for every ritual
- `thinking-errors.json` — 22 parent thinking errors
- `patterns.json` — 63 precise subpatterns
- `SCHEMA.json` — runtime ritual schema
- `PACKAGE_AUDIT.json` — package integrity checks
- `CHECKSUMS.sha256` — SHA-256 hashes
- `.txt` copy of the Framer component

## Consumer ritual contract

**Title → Play Time → Goal → Ritual → WIN → Mind Bend → Formula Flow → Safety**

The five-item archived advice arrays are deliberately excluded from the GitHub/runtime JSON payloads, and the Framer renderer contains no advice-card section.

## Routing

- 750 unique ritual IDs
- 22 canonical thinking errors
- 63 canonical precise subpatterns
- automatic routing only
- intent gate active
- semantic fit first; bubble/subpattern diversity used only among close-quality candidates
- no ritual repeats until all 750 IDs have been served, then the browser-local cycle resets
- Tier-3 support controls remain protected
- router storage version remains `v1900`

## Bubble distribution

GLITCH 108; DROP 107; STILL 107; PATCH 107; LOOPIE 107; RUSH 107; SYNC 107.

## Framer

Replace your current code component with `ThinkStill_UnifiedChat_v58_20_7_FINAL_RELEASE.tsx`, then set **Manifest URL** to the raw `manifest.json` URL.

Overlay video files are not included; the existing Framer property-control assets/URLs can remain connected.
