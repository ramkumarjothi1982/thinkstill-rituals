THINKSTILL — PRODUCTION LOCKED GITHUB FILES

RECOMMENDED:
1. Upload thinkstill-manifest.json to GitHub using the same filename/path your current chatbot already fetches.
2. If your chatbot only needs display/routing data and does not use Theory_Backbone,
   Mechanism_Fingerprint or Reveal_Internal, you can use thinkstill-runtime.json instead.
3. Do not rename JSON keys unless the Framer code is changed at the same time.
4. The .min.json files contain identical data with whitespace removed.

Canonical release:
ThinkStill MASTER 1000 — PRODUCTION LOCKED FINAL
Rituals: 1000
Encoding: UTF-8
