# ThinkStill flat GitHub package

Upload the contents of this package into your `thinkstill-rituals` repo.

## Structure
- `bubble-expressions/` -> contains all 448 expression WebP files in one folder
- `thinkstill_750_expression_map.json` -> expression mapping JSON updated for the flat folder structure

## Important
The JSON paths now use:
`bubble-expressions/patch_E01.webp`
instead of nested subfolders.
